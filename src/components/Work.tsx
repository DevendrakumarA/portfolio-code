import { useState, useCallback } from "react";
import "./styles/Work.css";
import WorkImage from "./WorkImage";
import { MdArrowBack, MdArrowForward } from "react-icons/md";

const projects = [
  {
    title: "Publisher Admin",
    category: "Affiliate Marketing / Publisher Management Platform",
    description:
      "A publisher management system for an affiliate marketing network where publishers can manage campaigns, track conversions, monitor earnings, and access performance analytics. The platform provides real-time reporting, offer management, and automated tracking for digital advertising campaigns.",
    tools: "Angular, Node.js, PHP, MySQL, HTML, CSS, JavaScript,MongoDB",
    image: "/images/publisher.png",
  },
  {
    title: "Advertiser Panel",
    category: "Affiliate Advertising & Campaign Management System",
    description:
      "An advertiser management platform designed to manage affiliate campaigns, offers, and publisher partnerships. Advertisers can create campaigns, monitor traffic sources, track ROI, and analyze campaign performance through a centralized dashboard.",
    tools: "Angular, Node.js, PHP, MySQL, HTML, CSS, JavaScript,MongoDB",
    image: "/images/dash1.png",
  },
  {
    title: "20+ Business Websites",
    category: "Full Stack Web Development",
    description:
      "Built and maintained 20+ websites using WordPress and modern web technologies with custom UI and backend integrations.",
    tools: "WordPress, Angular, Node.js, PHP, MySQL, HTML, CSS, JavaScript",
    image: "/images/websites.png",
  },
];

const Work = () => {
  const [currentIndex, setCurrentIndex] = useState(0);
  const [isAnimating, setIsAnimating] = useState(false);

  const goToSlide = useCallback(
    (index: number) => {
      if (isAnimating) return;
      setIsAnimating(true);
      setCurrentIndex(index);
      setTimeout(() => setIsAnimating(false), 500);
    },
    [isAnimating],
  );

  const goToPrev = useCallback(() => {
    const newIndex =
      currentIndex === 0 ? projects.length - 1 : currentIndex - 1;
    goToSlide(newIndex);
  }, [currentIndex, goToSlide]);

  const goToNext = useCallback(() => {
    const newIndex =
      currentIndex === projects.length - 1 ? 0 : currentIndex + 1;
    goToSlide(newIndex);
  }, [currentIndex, goToSlide]);

  return (
    <div className="work-section" id="work">
      <div className="work-container section-container">
        <h2>
          My <span>Work</span>
        </h2>

        <div className="carousel-wrapper">
          {/* Navigation Arrows */}
          <button
            className="carousel-arrow carousel-arrow-left"
            onClick={goToPrev}
            aria-label="Previous project"
            data-cursor="disable"
          >
            <MdArrowBack />
          </button>
          <button
            className="carousel-arrow carousel-arrow-right"
            onClick={goToNext}
            aria-label="Next project"
            data-cursor="disable"
          >
            <MdArrowForward />
          </button>

          {/* Slides */}
          <div className="carousel-track-container">
            <div
              className="carousel-track"
              style={{
                transform: `translateX(-${currentIndex * 100}%)`,
              }}
            >
              {projects.map((project, index) => (
                <div className="carousel-slide" key={index}>
                  <div className="carousel-content">
                    <div className="carousel-info">
                      <div className="carousel-number">
                        <h3>0{index + 1}</h3>
                      </div>
                      <div className="carousel-details">
                        <h4>{project.title}</h4>
                        <p className="carousel-category">{project.category}</p>
                        <div className="carousel-tools">
                          <span className="tools-label">Tools & Features</span>
                          <p>{project.tools}</p>
                        </div>
                        {/* <div className="carousel-tools">
                          <span className="tools-label">Description</span>
                          <p>{project.description}</p>
                        </div> */}
                      </div>
                    </div>
                    <div className="carousel-image-wrapper">
                      <WorkImage image={project.image} alt={project.title} />
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* Dot Indicators */}
          <div className="carousel-dots">
            {projects.map((_, index) => (
              <button
                key={index}
                className={`carousel-dot ${
                  index === currentIndex ? "carousel-dot-active" : ""
                }`}
                onClick={() => goToSlide(index)}
                aria-label={`Go to project ${index + 1}`}
                data-cursor="disable"
              />
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default Work;
